extends baseEmail

block content 
    h2= title
    h3 
    p Name : #{parentName}
    p Parent Email : #{parentEmail}
    p Child name : #{childName}
    p Child Age : #{childAge}
    p message : #{message}